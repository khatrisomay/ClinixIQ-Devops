apiVersion: v1
kind: Service
metadata:
  name: clinixiq-alertmanager-service
  namespace: clinixiq
  labels:
    app.kubernetes.io/name: clinixiq-alertmanager
spec:
  type: ClusterIP
  ports:
    - port: 9093
      targetPort: mesh
      name: mesh
  selector:
    app.kubernetes.io/name: clinixiq-alertmanager
