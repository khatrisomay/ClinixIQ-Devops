apiVersion: v1
kind: Service
metadata:
  name: clinixiq-prometheus-service
  namespace: clinixiq
  labels:
    app.kubernetes.io/name: clinixiq-prometheus
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: web
      name: web
  selector:
    app.kubernetes.io/name: clinixiq-prometheus
