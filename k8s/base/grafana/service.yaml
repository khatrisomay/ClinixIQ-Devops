apiVersion: v1
kind: Service
metadata:
  name: clinixiq-grafana-service
  namespace: clinixiq
  labels:
    app.kubernetes.io/name: clinixiq-grafana
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: clinixiq-grafana
