apiVersion: v1
kind: Service
metadata:
  name: clinixiq-backend-service
  namespace: clinixiq
  labels:
    app.kubernetes.io/name: clinixiq
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: clinixiq
    app.kubernetes.io/component: backend
