apiVersion: v1
kind: Service
metadata:
  name: clinixiq-frontend-service
  namespace: clinixiq
  labels:
    app.kubernetes.io/name: clinixiq
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: clinixiq
    app.kubernetes.io/component: frontend
